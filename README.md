# Kettlebelt

Kettlebell workouts built around the bells you own: ready-made workouts by body part, a "build your own" generator and an exercise library.

Expo SDK 57 · Expo Router · TypeScript. All data is mock data for now (no backend).

## Run

```bash
npm install
npx expo start
```

Open in Expo Go (scan the QR code), or press `i` / `a` for a simulator, or `w` for web.

## Testing onboarding again

The app remembers you after onboarding. In development, the Log tab has a “Reset onboarding (dev only)” button.

## Structure

```
src/
  app/                  routes (Expo Router)
    _layout.tsx         fonts, splash, root stack
    index.tsx           redirects to onboarding or tabs
    onboarding/         welcome → pick your bells → where do you start
    (tabs)/index.tsx    2 · Today
    (tabs)/workouts.tsx ready-made workouts by body part + "Build your own"
    generator.tsx       build your own: body part, time, goal
    routine.tsx         workout detail: exercises, reps, which bell, totals
    workout.tsx         7 · Gym-floor mode (working EMOM timer)
    summary.tsx         8 · Session summary
    (tabs)/library.tsx  exercise library: search, movement and level filters
    exercise/[id].tsx   exercise detail: start/finish photo loop, muscles, steps
    (tabs)/log.tsx      placeholder history
  components/           Bell, Icon, shared UI (Button, Chip, Card, Screen…)
  data/exercises.ts     63 kettlebell exercises (generated by scripts/build-exercises.py from free-exercise-db, public domain)
  data/workouts.ts      ready-made workouts, EMOM helpers, generator
  data/mock.ts          example Log history
  store/bells.tsx       owned bells, level, days/week, onboarding flag (saved with AsyncStorage)
  theme.ts              colours (competition-bell palette), fonts
```

## Next steps

- More ready-made workouts, reviewed by a kettlebell coach
- Bundle or self-host the exercise photos (they load from GitHub for now)
- Save sessions to the Log tab
# kettlebell
